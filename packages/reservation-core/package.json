{
  "name": "@reservation-core/domain",
  "version": "0.1.0",
  "description": "Layer-2 reservation domain on the @tenantkit/* kernel: pure courses/sessions/capacity logic and the omluvenka (excuse → makeup-credit → redeem) engine. Vendor-free, no I/O.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./credits": "./src/credits/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json --outDir dist --declaration"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.0.0"
  }
}
